const Homepage = () => {
    return (
        <div style={{ display: "flex", justifyContent: "center" }}>
            <h1
                style={{
                    display: "flex",
                    color: "black",
                    marginTop: "200px",
                    fontFamily: "TITLE",
                }}
            >
                A One Stop Shop
            </h1>
        </div>
    );
};

export default Homepage;