const Homepage = () => {
    return (
        <div style={{ display: "flex", justifyContent: "center", background: "#93cee3" }}>
            <h1
                style={{
                    display: "flex",
                    color: "white",
                    marginTop: "70px",
                    fontSize: "3rem",
                }}
            >
                A One Stop Shop
            </h1>
        </div>
    );
};

export default Homepage;